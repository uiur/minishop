syntax = "proto3";

import "rpc/order/order_item.proto";
package rpc.order;

message OrderResponse {
  string id = 1;
  enum Status {
    CART = 0;
    ORDEDED = 1;
  }
  Status status = 2;
  repeated OrderItem order_items = 3;
}
